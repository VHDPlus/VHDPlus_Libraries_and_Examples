	component ISSP is
		port (
			probe  : in  std_logic_vector(31 downto 0) := (others => 'X'); -- probe
			source : out std_logic_vector(7 downto 0)                      -- source
		);
	end component ISSP;

